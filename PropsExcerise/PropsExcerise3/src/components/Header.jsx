// eslint-disable-next-line no-unused-vars
import React from "react";

const Header = (props) => {
    return (
        <>
            <h1>Header content : {props.heading}</h1>
        </>
    );
};

export default Header;
